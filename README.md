# 🛒 Tienda en Línea con Microservicios (FastAPI)

Este proyecto muestra una arquitectura básica de microservicios usando **Python + FastAPI**.

## 📦 Estructura

- `usuarios/` → Maneja los usuarios.
- `productos/` → Maneja los productos.
- `pedidos/` → Crea pedidos consultando los otros servicios.
- `frontend/` → Interfaz web sencilla para probar los servicios.

## 🚀 Cómo ejecutar

1. Instala las dependencias:
   ```bash
   pip install -r usuarios/requirements.txt
   pip install -r productos/requirements.txt
   pip install -r pedidos/requirements.txt
   ```
